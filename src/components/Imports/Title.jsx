import React from 'react'
import {makeStyles} from '@material-ui/core/styles'


function Title() {
    const useStyles = makeStyles((theme) => ({
        bestSell_title:{
        padding:'25px',
        color:'#666',
        // fontSize:'40px',
        fontFamily: `'Playfair Display SC', serif`
        },
    }))
    const displayTitle = (title_name) =>{

    }
    return (
        <div>
            
        </div>
    )
}

export default Title
